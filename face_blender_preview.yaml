# 默认预览配置：`uv run python run.py`（可用 --config 指定其它路径）
# path: SRanipal 37 维 CSV；内置 MetaHuman 头模，映射为 ARKit 驱动
path: data/sample_data.csv

# 可选
# fps: 15
# view_scale: 1.0
# window_width: 960
# window_height: 540
# head: null
# extra_meshes: null
# dual_view: false   # true 时额外打开侧视窗口
# fbx: null          # 自定义 FBX 路径；null 则用 models/Metahuman_Head.fbx（若存在）
